{
  "name": "argon-bootstrapper",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun run build && bun test",
    "dev": "bun --watch src/server.ts",
    "build": "cd ui && vite build",
    "types": "tsc --noEmit && vue-tsc --noEmit -p ui/tsconfig.json"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "@vitejs/plugin-vue": "^6.0.8",
    "playwright": "^1.62.1",
    "typescript": "~7.0.0",
    "vite": "^8.2.2",
    "vue-tsc": "^3.3.11"
  },
  "dependencies": {
    "elysia": "^1.4.29",
    "vue": "^3.5.41"
  }
}
